import type { Metadata } from "next";

export const metadata: Metadata = {title:"Long Tone | Cookie",description:"An interactive flute score viewer with playback, drones, fingering, rhythm helpers, and teaching markup."};

export default function LongToneLayout({children}:{children:React.ReactNode}){return children}
